# docs/js/data

Dados JavaScript importáveis diretamente por módulos do front-end.

- `terminalTexts.js`: frases usadas no efeito de terminal/typewriter.

Dados editoriais em JSON devem ficar em `docs/data` para poderem ser consumidos por `core.fetchJSON`.
