# Raiz do projeto

Arquivos JavaScript na raiz atuam como pontos de entrada de desenvolvimento.

- `start.js`: gerenciador local para subir API, frontend estático, keep-alive experimental e verificações (`npm test`).

Use `npm run dev` para iniciar API + frontend e `npm test` para validações rápidas.
